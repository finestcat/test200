<html>
    <body>
        <H2>(<%= nItems %>) is found. There are (<%= nItems %>) inventory items. </H2>
        <ul>
            <% for (var i of items) { %>
            <li>Inventory ID: <a href="/details?_id=<%= i._id %>"><%= i.inventoryID %></a></li>
            <% } %>
        </ul>
        <a href="/">Back to home.</a>
    </body>
</html>
